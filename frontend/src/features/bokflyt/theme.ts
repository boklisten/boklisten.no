/**
 * The page's palette, for props that need a colour value. The CSS module declares
 * the same colours as custom properties for everything styled in CSS.
 */
export const BOKFLYT_COLORS = {
  deep: "#1b5a7a",
  light: "#6fa3cf",
  ink: "#15303f",
} as const;
