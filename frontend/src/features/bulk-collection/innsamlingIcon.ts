import type { Icon } from "@tabler/icons-react";

import { BOOK_EVENT_APPEARANCE } from "@/shared/components/bookEventAppearance";

/** Innsamling is a return, so it wears the same glyph as a return in a book's history. */
export const InnsamlingIcon: Icon = BOOK_EVENT_APPEARANCE.return.icon;
